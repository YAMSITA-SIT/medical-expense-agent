const API_BASE_URL=(import.meta.env.VITE_API_BASE_URL||'http://127.0.0.1:8001').replace(/\/$/,'')
async function request<T>(path:string,init?:RequestInit):Promise<T>{let response:Response;try{response=await fetch(`${API_BASE_URL}${path}`,init)}catch{throw new Error('APIサーバーに接続できません。バックエンドが起動しているか確認してください。')}if(!response.ok){let detail='';try{const b=await response.json();detail=typeof b.detail==='string'?b.detail:''}catch{/* response may not be JSON */}throw new Error(detail||`APIでエラーが発生しました（${response.status}）`)}return response.json() as Promise<T>}
export const api={baseUrl:API_BASE_URL,health:()=>request<{status:string}>('/health'),extract:(file:File)=>request<import('./types').OcrResponse>('/v2/documents/extract',{method:'POST',headers:{'Content-Type':file.type},body:file}),validate:(body:unknown)=>request<import('./types').CaseResponse>('/v2/cases/validate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),evaluateV2:(body:unknown)=>request<import('./types').CaseResponse>('/v2/cases/evaluate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),evaluateV3:(body:unknown)=>request<import('./types').BenefitResponse>('/v3/benefits/evaluate-ocr',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})}
